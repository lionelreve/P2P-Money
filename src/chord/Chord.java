package chord;

public class Chord implements Chordable {

	@Override
	// ask node n to find the successor of id
	public int find_successor(int id) {
		// TODO Auto-generated method stub
		/*
	    if (id  (n, successor] ) // Yes, that should be a closing square bracket to match the opening parenthesis. It is a half closed interval.
			return successor;
	    else
	     // forward the query around the circle
	     n0 = closest_preceding_node(id);
	     return n0.find_successor(id);
		*/
		return 0;
	}


}
